"use client";
import { useState } from "react";
export function ExternalImportPanel() { const [file, setFile] = useState<File | null>(null); const [message, setMessage] = useState(""); const [busy, setBusy] = useState(false);
  async function submit() { if (!file) return; setBusy(true); setMessage(""); try { const csv = await file.text(); const response = await fetch("/api/market/tenbi", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ csv }) }); const body = await response.json(); setMessage(response.ok ? `Tenbi ${body.imported}건 반영 · 오류 ${body.rejected?.length ?? 0}건` : body.message); } catch { setMessage("가져오기에 실패했습니다."); } finally { setBusy(false); } }
  return <section className="discovery-command__panel" aria-labelledby="external-import-title"><header><div><p className="eyebrow">EXTERNAL MARKET SIGNALS</p><h2 id="external-import-title">Tenbi · TikTok 신호 가져오기</h2><span>Tenbi CSV를 원본 digest와 오류행 이력으로 보존합니다. TikTok은 Creator Center 공식 CSV를 fixture adapter로 검토합니다.</span></div></header><div className="discovery-command__hero-actions"><label htmlFor="tenbi-csv">Tenbi CSV</label><input id="tenbi-csv" type="file" accept=".csv,text/csv" onChange={(event) => setFile(event.target.files?.[0] ?? null)} /><button type="button" disabled={!file || busy} onClick={() => void submit()}>{busy ? "가져오는 중" : "가져오기"}</button></div>{message && <p className="notice">{message}</p>}</section>;
}
