# v10.2.0 — Command Center + Workflow Automation

- v10.1 Command Center와 v10.2 Workflow Automation 통합
- `/api/os/dashboard` 통합 조회 API 추가
- `/api/os/commands` 명령 생성 및 조회 API 추가
- `/api/os/commands/[id]` 상태 변경 API 추가
- `os_command_runs`, `os_notifications` 테이블 추가
- `/os` 화면을 조회형 Dashboard에서 실행형 Control Tower로 전환
