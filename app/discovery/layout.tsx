import type { Metadata } from "next";
export const metadata: Metadata = { title: "1-1. 시장 후보 발굴·의사결정 | 공감라인 AI" };
export default function Layout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }
