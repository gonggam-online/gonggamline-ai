import type { Metadata } from "next";
export const metadata: Metadata = { title: "2-1. 쿠팡 판매 경쟁력 분석 | 공감라인 AI" };
export default function Layout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }
