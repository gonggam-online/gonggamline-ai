import type { Metadata } from "next";
export const metadata: Metadata = { title: "4-1. 물류·재고 통합 Workspace | 공감라인 AI" };
export default function Layout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }
